def lambda_handler(event, context):
    # define 3 metrics for web resource
    # metric 1 - availability
    # metric 2 - latency
    # metric 3 - 
    # select a web resource to monitor
    return {
        'statusCode': 200,
        'body': 'Hello from WHLambda!'
    }